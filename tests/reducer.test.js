import { get } from "lodash";
import { set } from "lodash";

export default function(action, state) {
    switch (action.type) {
        case SOMETHING:
            break;
        default:
            break;
    }
}